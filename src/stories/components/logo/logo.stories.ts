import { Meta } from '@storybook/react';
import { Logo } from './logo';

const meta: Meta<typeof Logo> = {
  title: 'HomeWork/Logo',
  component: Logo,
  tags: ['autodocs'],
};

export default meta;

export const ShowedLogo = {

};
