import type { Meta } from '@storybook/react';
import { Layout } from './layout';

const meta: Meta<typeof Layout> = {
  title: 'HomeWork/Layout',
  component: Layout,
  parameters: {
    layout: 'fullscreen',
  },
};

export default meta;

export const ShowedLayout = {};
